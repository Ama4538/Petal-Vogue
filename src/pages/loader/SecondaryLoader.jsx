function SecondaryLoader() {
    return (
        // Loader for the page refresh when data is being loaded
        <section className="secondaryloader">
            <div class="secondaryloader__animation"><div></div><div></div><div></div></div>
        </section>
    )
}

export default SecondaryLoader;